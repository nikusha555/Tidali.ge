<!DOCTYPE html>
<html lang="en">
<%- include('../../partials/head') %>

    <body>
        <div class="wrapper">
            <%- include('../../partials/side-navbar') %>
                <%- include('../../partials/header') %>

                    <div class="page-content">
                        <div class="page-container">
                            <div class="d-flex justify-content-center px-2">
                                <div class="col-12 col-sm-10 col-md-8 col-lg-6">
                                    <div class="card">
                                        <div class="card-header border-bottom border-dashed d-flex align-items-center">
                                            <h4 class="header-title">ჩვენს შესახებ რედაქტირება</h4>
                                        </div>

                                        <div class="card-body pt-3 pb-2">
                                            <!-- ✅ Edit Form -->
                                            <form action="/edit-about" method="POST" enctype="multipart/form-data">

                                                <div class="row">
                                                    <!-- Image on the left -->
                                                    <div class="col-md-6 text-center mb-3">
                                                        <% if (about.img_url) { %>
                                                            <img src="<%= about.img_url %>" alt="Current Image"
                                                                class="img-fluid rounded shadow"
                                                                style="max-height: 250px;">
                                                            <% } %>

                                                                <!-- Upload new image trigger -->
                                                                <div class="dz-message needsclick mt-3"
                                                                    id="uploadTrigger" style="cursor:pointer;">
                                                                    <i class="ti ti-cloud-upload h1 text-muted"></i>
                                                                    <h4>ატვირთეთ ახალი სურათი</h4>
                                                                    <input name="image" id="imageInput" type="file"
                                                                        accept="image/*" hidden />
                                                                    <img id="imagePreview" src="" alt="Preview"
                                                                        class="img-fluid mt-3 d-none"
                                                                        style="max-height: 200px;" />
                                                                </div>
                                                    </div>

                                                    <!-- Video on the right -->
                                                    <div class="col-md-6 text-center mb-3">
                                                        <% if (about.video_url) { %>
                                                            <video id="existingVideo" controls
                                                                class="img-fluid rounded shadow mb-3"
                                                                style="max-height: 250px;">
                                                                <source src="<%= about.video_url %>" type="video/mp4">
                                                                Your browser does not support the video tag.
                                                            </video>
                                                            <% } %>

                                                                <!-- Custom upload trigger -->
                                                                <div class="dz-message needsclick mt-3"
                                                                    id="videoUploadTrigger" style="cursor:pointer;">
                                                                    <i class="ti ti-cloud-upload h1 text-muted"></i>
                                                                    <h4>ატვირთეთ ვიდეო</h4>
                                                                    <input name="video" id="videoInput" type="file"
                                                                        accept="video/*" hidden />
                                                                </div>

                                                                <!-- Live video preview -->
                                                                <!-- <video id="videoPreview" controls
                                                                    class="img-fluid rounded shadow mt-3 d-none"
                                                                    style="max-height: 250px;">
                                                                    <source id="videoSource" src="" type="video/mp4">
                                                                    Your browser does not support the video tag.
                                                                </video> -->
                                                    </div>
                                                </div>


                                                <!-- Form inputs -->
                                                <div class="mt-3">
                                                    <input type="text" class="form-control mb-3" name="title"
                                                        placeholder="სათაური" value="<%= about.title %>" required>


                                                    <textarea class="form-control mb-3" name="content" rows="5"
                                                        placeholder="ტექსტი" required><%= about.content %></textarea>
                                                </div>

                                                <!-- SEO Settings Collapse Toggle -->
                                                <div class="text-end mb-2">
                                                    <a class="btn btn-sm btn-outline-secondary"
                                                        data-bs-toggle="collapse" href="#seoSettings" role="button"
                                                        aria-expanded="false" aria-controls="seoSettings">
                                                        SEO პარამეტრები ▼
                                                    </a>
                                                </div>

                                                <!-- SEO Settings (Initially Collapsed) -->
                                                <div class="collapse" id="seoSettings">
                                                    <div class="card card-body border rounded shadow-sm mb-3">
                                                        <div class="form-group mb-3">
                                                            <label for="meta_description">მეტა
                                                                აღწერა</label>
                                                            <textarea class="form-control" name="m_description"
                                                                id="m_description" rows="2"
                                                                placeholder="მეტა აღწერა..."><%= about.m_description %></textarea>
                                                        </div>

                                                        <div class="form-group mb-0">
                                                            <label for="meta_keywords">მეტა საკვანძო
                                                                სიტყვები</label>
                                                            <input type="text" class="form-control" name="m_keywords"
                                                                id="m_keywords" placeholder=""
                                                                value="<%= about.m_keywords %>">
                                                        </div>
                                                    </div>
                                                </div> <br>
                                                <div class="d-flex justify-content-center">
                                                    <button type="submit" class="btn btn-success">შენახვა</button>
                                                </div>
                                        </div>
                                        </form>
                                    </div> <!-- end card-body -->
                                </div> <!-- end card -->
                            </div> <!-- end col -->
                        </div>
                    </div>
        </div>
        </div>

        <!-- JS to handle file preview -->
        <script>
            const trigger = document.getElementById('uploadTrigger');
            const input = document.getElementById('imageInput');
            const preview = document.getElementById('imagePreview');

            trigger.addEventListener('click', () => input.click());

            input.addEventListener('change', () => {
                const file = input.files[0];
                if (file && file.type.startsWith('image/')) {
                    const reader = new FileReader();
                    reader.onload = (e) => {
                        preview.src = e.target.result;
                        preview.classList.remove('d-none');
                    };
                    reader.readAsDataURL(file);
                }
            });






            const videoTrigger = document.getElementById('videoUploadTrigger');
            const videoInput = document.getElementById('videoInput');
            const videoPreview = document.getElementById('videoPreview');
            const videoSource = document.getElementById('videoSource');

            videoTrigger.addEventListener('click', () => videoInput.click());

            videoInput.addEventListener('change', () => {
                const file = videoInput.files[0];
                if (file && file.type.startsWith('video/')) {
                    const videoURL = URL.createObjectURL(file);
                    videoSource.src = videoURL;
                    videoPreview.load();
                    videoPreview.classList.remove('d-none');

                    // Optional: Hide existing video if previewing new one
                    const existingVideo = document.getElementById('existingVideo');
                    if (existingVideo) existingVideo.style.display = 'none';
                }
            });
        </script>

        <%- include('../../partials/footer') %>
    </body>

</html>